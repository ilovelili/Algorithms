package algorithm

import (
	"./algorithmutil"

	"./sort"
)

var (
	sorter *sort.Sorter
)

// BubbleSort BubbleSort entry
func BubbleSort(arrayzor []algorithmutil.IComparable) []algorithmutil.IComparable {
	return sorter.BubbleSort(arrayzor)
}
